---
title: source-artifact-required
type: note
permalink: cs2-vibesignatures/source-artifact-required
---

# source-artifact-required

## Overview
`.github/workflows/source-artifact-required.yml` is the PR / Merge Queue gate workflow. Its first job `bind-source-artifact-plan` loads a default-branch trusted planner, analyzes the untrusted prospective merge tree read-only, and emits a plan bound to the exact merge tree that routes all downstream validation (`light` / `full` / `bootstrap_required`). It triggers on `pull_request_target` + `merge_group` and only runs on `HLND2T/CS2_VibeSignatures`.

## Responsibilities
- Checkout the prospective tree as data only (`persist-credentials: false`): PR events use `refs/pull/{N}/merge`, merge_group uses its head SHA; nothing from the tree is executed.
- Resolve immutable event identity (`identity` step, pwsh): base/head/merge SHA plus `same-repository`; merge_group proves PR membership through the GitHub API (or the `gh-readonly-queue/.../pr-N-<sha>` head-ref regex) and accepts only same-repo PRs targeting the default branch; PR base must be the default branch; the checked-out HEAD must equal the event merge SHA.
- Checkout the trusted planner from `base-sha` into `.trusted-planner`; the bridge (`trusted_pr_context.py` + `trusted_artifact_pr.py`) is mandatory after the source-owned cutover — a missing bridge fails the job.
- Build the plan: `trusted_pr_context.py build` -> trusted context JSON, then `trusted_artifact_pr.py plan` -> plan JSON; publish job outputs and upload the plan as the immutable artifact `trusted-source-artifact-plan-<run_id>-<run_attempt>` (7-day retention).
- Route downstream: `bootstrap_required` -> `bootstrap-new-gamever` (pull_request_target + same-repo only); `light` -> `test-prospective-tree`; `full` -> `test-prospective-tree` plus per-GAMEVER `validate-full` matrix -> `pr-self-runner.yml` (same-repository only; forks fail closed).
- Terminal gates: `source-artifact-required` (stable required check aggregating binding/bootstrap/tests/full) and `pr-validate-merge-group` for the merge queue.

## Plan document (trusted_artifact_pr.py)
- Fields: `schema_version`, `event_kind`, `mode`, `base_sha`/`head_sha`/`merge_sha`/`merge_tree_sha`, `trusted_context_sha256`, `configured_game_versions`, `affected_game_versions`, `download_sha256`, `sdk_gitlink_sha`, base/merge analysis-source inventories, `changed_paths`, `impact` (release/gamedata/cpp/pages path-prefix flags), per-GAMEVER `game_versions` reports (`invalidated_paths`, `bootstrap_required`, `prior_gamever`), and the `plan_sha256` digest.
- Mode selection: any version with `bootstrap_required` -> `bootstrap_required`; any version with `invalidated_paths` -> `full`; otherwise `light`.
- `validate_trusted_artifact_plan` re-derives `plan_sha256` and rejects modes outside the three-value set; downstream workers reload and re-verify the plan, so plan drift fails closed.

## Involved Files & Symbols
- `.github/workflows/source-artifact-required.yml` - jobs `bind-source-artifact-plan`, `bootstrap-new-gamever`, `test-prospective-tree`, `validate-full`, `source-artifact-required`, `pr-validate-merge-group`.
- `trusted_pr_context.py` - trusted event context binding (repo root, event kind, base/head/merge refs, context digest).
- `trusted_artifact_pr.py` - `build_trusted_artifact_plan`, `validate_trusted_artifact_plan`, `load_trusted_artifact_plan`, `prepare_isolated_rebuild` (requires a full plan).
- `source_artifact_policy.yaml` - `mode: source-owned`, `artifact_root: bin_artifacts`; loaded from the trusted side.
- `.github/workflows/pr-self-runner.yml`, `.github/workflows/bootstrap-new-gamever-artifacts.yml` - routed downstream workers.

## Architecture
```text
pull_request_target / merge_group event
  -> checkout prospective tree as data (persist-credentials: false)
  -> immutable identity (base/head/merge SHA, same-repository)
  -> checkout base-owned planner at base-sha
  -> trusted context + plan bound to exact merge tree (plan_sha256)
  -> mode routing: light | full | bootstrap_required
  -> hosted tests (+ self-hosted full validation) or new-GAMEVER bootstrap
  -> stable required check source-artifact-required / pr-validate
```

## Design Intent
- `pull_request_target` trust boundary: the event runs base-branch workflow code with secret read access, so the PR tree is data only and every executed planner/policy byte comes from the immutable base SHA — a PR cannot influence its own validation routing by editing workflows, planner code, or policy.
- Immutable binding: the plan is bound to the exact prospective tree (`merge_tree_sha`) and self-hashed (`plan_sha256`); downstream jobs receive both via job outputs and the uploaded plan artifact and fail closed on drift, so planned tree = validated tree = merged tree (the merge queue revalidates the exact tree).
- Fail-closed everywhere: missing trusted bridge, fork + full mode, obsolete bootstrap-published head, invalid mode.
- Stable required checks: the multi-branch pipeline exposes fixed check names to branch protection.

## Dependencies
- Same-repository PR for full validation; fork PRs needing isolated IDA analysis must be mirrored to a same-repository branch.
- Required Workflow / ruleset trust root outside the repository so prospective workflow edits cannot self-report success.
- Related notes: [[post_change_candidate_lifecycle]] (lifecycle overview), [[pr-self-runner]] (full-validation worker), [[binary_lock]], [[project_overview]].

## Notes
- [fact] Empirically verified (two-version fixture, 2026-09-06): with `configs/14177.yaml` + `configs/14178b.yaml`, a shared-analysis change yields `mode=full, affected_game_versions=["14178b"]` and empty invalidation for the old version. Consequence of the latest-only maintenance scope: a PR editing only `bin_artifacts/<old GAMEVER>/**` routes to `light` mode — no producer rebuild validation for old-version artifact edits (same behavior with or without the workflow pin).

- [fact] Job outputs consumed downstream: `validation-mode`, `plan-sha256`, `merge-tree-sha`, `base-sha`, `head-sha`, `merge-sha`, `bootstrap-gamever`, `bootstrap-prior-gamever`, `plan-artifact-name`, `affected-versions`, `same-repository`.
- [fact] `bootstrap_required` always fails the terminal check by design: the bootstrap publisher appends `bin_artifacts/<GAMEVER>/**` to the bound PR head, making the validated head obsolete; the PR must be revalidated after `pull_request.synchronize`.
- [fact] Since commit `445e7312` (2026-09-06) the `validate-full` matrix derives unconditionally from `plan.affected_game_versions`; the hardcoded `["14178b"]` literal added by `92592fc9` was removed as redundant. Latest-only scoping lives entirely in the trusted planner (`maintained_versions` in `trusted_artifact_pr.py`, from `440f46e7`): every invalidation path is gated on `maintained`, so full mode always affects exactly the newest configured GAMEVER and GAMEVER rotation follows `configs/` without a workflow bump. `prepare_isolated_rebuild` still fails closed when the matrix gamever is not the plan's single affected target.
- [fact] `test-prospective-tree` runs `format_repo_files.py --check` plus all `tests/run_test_suite.py` suites (unit, repository-contract, redis-integration, release-integration, all) on the merge SHA with recursive submodules.

- [fact] Since the base-inherited-selected bridge (branch `dev-base-inherited-selected`, 2026-09-06) the plan schema is **4** and carries a top-level `execution_strategy` (`fresh-full-v1` default | `base-inherited-selected-v1`), sourced from the base-owned `source_artifact_policy.yaml` optional key `execution_strategy`. Each maintained GAMEVER report now partitions outputs into `execute_groups` / `execute_nodes` / `inherit_paths` / `inherited_absent_groups` / `removed_paths` (old `affected_producer_groups` / `selected_alternative_nodes` names removed; consumers updated: verifier, `new_gamever_artifact.py` bootstrap check, tests). The execution closure adds same-session prerequisite back-propagation. `pr-self-runner.yml` routes its execute step on `preparation.execution_strategy`: selected runs `ida_analyze_bin.py -selected_execution <manifest>` against a seeded actual root; fresh-full keeps `-force_all` with an empty root. Selected routing is enabled by the independent policy activation after Phase-D (2026-09-07); the parser still defaults to fresh-full when the key is absent. The PR worker explicitly uses CS2VIBE_STRING_MIN_LENGTH=4. Selected evidence cannot impersonate full: independent report domain `source2-selected-execution:v1`, and `release_artifact_rebuild.py` still demands force-all v2.

- [fact] Phase-D evidence: run 34031704261 passed PR/new-symbol+finder (6 groups), artifact-only (3), cross-stage (14), and fresh-full (3529); the shared-runtime selected sample retained 2195 succeeded nodes but skipped one Linux producer via the in-session skip_if_exists check. PR #931 fixed the missing `and not force_all` guard, covering both pre-existing and in-session marker cases. The records-only verifier extraction preserves production report identity/validity requirements.
- [fact] One-off continuation run 34067782997 restored the exact original warm generation and binary lock, retained the old successful evidence, and retried only CFlashbangProjectile_Spawn_NetworkStateChangedNotify on Linux (53.203 seconds). All 3529 groups are covered and the repaired 3526-file inventory equals the validated full baseline. The new proof is explicitly cross-run migration evidence, not a production PR/release attestation; the original failed report remains unchanged.
- [fact] Measured PR sample: producer 109.578 s, prepare/restore/producer/verify/downstream total 466.687 s; full baseline 7204.719 s / 7604.359 s. These totals exclude shared planning, runner provisioning and queue time. Shared-runtime changes still select all groups. Continuous base inheritance risk and release fresh-full requirements remain as documented.
- [fact] PR #926 merged on 2026-09-07 at e80b418a4689abdf3de1b569217ffb9511a8053c, satisfying the cleanup gate. The subsequent cleanup patch removes phase-d-validation.yml, phase_d_validation.py, phase_d_resume.py and temporary helper tests/registration. The executor fix, permanent regression test, selected policy, handoff and evidence links are retained.
- [fact] Routing is path-predicate driven, not impact-driven, and the predicate surface is much wider than the artifact-producing surface. `bin_artifacts/<GAMEVER>/**` and `configs/<GAMEVER>.yaml` reach invalidation only through the snapshot/config delta for the **maintained (latest) GAMEVER** (`trusted_artifact_pr.py:681,888-897`; `gamesymbol_snapshot_lib/pr_validation.py:316,319`). `ida_preprocessor_scripts/**` and `.claude/skills/<skill referenced by configs>/**` map to producer nodes (`pr_validation.py:124,167,193`). Force-full overrides live in `trusted_artifact_pr.py:771-782` (new/removed GAMEVER, `shared_analysis_changed`) and `:784-790` (download/binary-lock identity). `_is_shared_analysis_runtime_path` (`:92-98`) treats **any root-level `*.py`** as shared analyzer runtime, so editing `release_artifact_rebuild.py`, `process_*.py`, `gamedata_*.py`, `format_repo_files.py` etc. forces a ~2h force-all rebuild for every maintained GAMEVER. **Superseded 2026-09-08 by PR #950 (#946):** the shared-runtime force-full, the sig-finder/unmapped broad rebuilds, and the contract-version fallback were removed; routing is now maintained-gamesymbol/effective-generator driven (see the #950 entry below).
- [fact] Orthogonal to routing, `build_trusted_artifact_plan` hard-fails (before mode is computed) on any changed path matching `_is_trust_root_path` (`trusted_artifact_pr.py:684-692`, `:101-103`; roots = `TRUSTED_FILE_PATHS` in `trusted_pr_context.py:28-53` plus `.github/workflows/`). Consequence: a PR cannot modify `trusted_artifact_pr.py`, the validation workflows, or `artifact_diagnostics.py` — the base-owned planner refuses with "requires an independently merged bridge update". Empirical sample: PR #945 (run 34214743675 / job 102023745074) failed exactly this way on `.github/workflows/pr-self-runner.yml`, `artifact_diagnostics.py`, `trusted_artifact_pr.py`, yet merged with the required check FAILURE and no merge_group run — ruleset `source-artifact-required-main` (id 22160809, `refs/heads/main`, requires `pr-validate` + `source-artifact-required`) carries a User `bypass_mode: always` actor. Treat the guard as advisory-in-practice until the bypass usage is addressed. **Superseded 2026-09-08 by PR #950 (#946): the blanket trust-root guard was removed; a PR modifying validators/workflows now plans normally and routes `light` while every executed planner byte still comes from the base SHA.**
- [decision] #946 proposes narrowing full-validation triggers to only `configs/**`, `bin_artifacts/**`, `.claude/skills/find-*/**`, `ida_preprocessor_scripts/**`, on the rationale that full-validation verifies PR-submitted gamesymbols and their generator skills/preprocessors — not infrastructure reliability. Open questions recorded there: manual binary-identity edits to an already-configured GAMEVER (`download.yaml` existing-tag manifest or `binary_locks/<ver>.json`; a *new* GAMEVER instead routes to `bootstrap_required`, not full, because the bump commit appends the download tag and adds `configs/<tag>.yaml` together, with `binary_locks/<tag>.json` staged as an add by `bump_download_candidate.py:257,364,522`), non-latest `bin_artifacts/**` handling (cf. #847), whether `.claude/skills/find-*` needs a configs reference, whether `sig-finder.md` counts, and whether the trust-root guard stays. `test-prospective-tree` (format + all suites) is explicitly out of scope for narrowing.
- [fact] `light` still runs `format_repo_files.py --check` plus all five `tests/run_test_suite.py` suites (`source-artifact-required.yml:217-240`); it only skips the self-hosted IDA rebuild.
- [fact] New-GAMEVER routing is bootstrap, not full: `download.yaml` is an append-only catalog and every bump lands the tag and `configs/<tag>.yaml` in the same commit (`60ae5e57`, `ec9bff07`), with `binary_locks/<tag>.json` staged as an add by `bump_download_candidate.py:257,364,522`. That makes `is_new=True` -> missing required artifacts -> `bootstrap_required`, and mode priority (`trusted_artifact_pr.py:893-897`) puts it above `full`, so `validate-full` is skipped (`source-artifact-required.yml:246`) and `bootstrap-new-gamever` -> `bootstrap-new-gamever-artifacts.yml` runs instead. Consequently the `binary_identity_changed` force-full branch (`trusted_artifact_pr.py:784-790`) only bites already-configured versions — e.g. `bee4550d` hand-edited an existing tag to add `major_update: true`, which forces full even though that field only steers `prior_gamever` selection (`:884`).
- [fact] Non-latest GAMEVER edits are NOT policy-rejected by the planner (measured 2026-09-08 with git-plumbing probe commits, `refs/probe/*`, deleted afterwards). A comment-only edit to `configs/14167.yaml` yielded `mode=light`, `affected_game_versions=[]`, all 17 version reports `invalidated_paths=[]` — because every invalidation branch is gated on `maintained` (`trusted_artifact_pr.py:753-812`). Adding a required output (`SDL_ProbeNonexistent.{platform}.yaml`) to the same old config did fail, but with the consistency error `missing required Git artifacts for 14167: SDL3/SDL_ProbeNonexistent.windows.yaml` from `_tree_artifact_inventory` (`:374-408`) — a config↔artifact consistency check, not a non-latest rule. So only consistency-breaking old-version edits are rejected; internally consistent ones (prerequisite/input edits, replacing existing artifact bytes) silently route to `light`. The only non-latest constraint today is review gate 5 in `.claude/skills/resolve-pr-conflict/references/review-pr.md:80-83,94` (introduced by `b1e9f043`, 2026-09-02); #847's request (fail fast on non-latest config edits) was never implemented in automation — `maintained_versions` latest-only scoping (`440f46e7`, 2026-09-03) ignores rather than rejects.
- [decision] 2026-09-08 agreed scope for #946 (owner-approved): (1) full-validation triggers narrow to `configs/**`, `bin_artifacts/**`, `.claude/skills/find-*/**`, `ida_preprocessor_scripts/**`; (2) non-latest `configs/<ver>.yaml` and `bin_artifacts/<ver>/**` edits must fail closed instead of silently routing to `light`, promoting review gate 5 into a planner hard gate and closing #847's automation half; (3) manual binary-identity edits to an already-configured GAMEVER (`download.yaml` existing-tag manifest or `binary_locks/<ver>.json`) must fail closed and go through the bump flow rather than forcing a full rebuild. Still open: whether `.claude/skills/find-*` requires a configs reference, whether `sig-finder.md` counts as a trigger, and whether the trust-root guard stays.

## Issue 946 refined decisions (2026-09-08; supersedes earlier four-path proposal)

- [decision] Owner interview completed and https://github.com/HLND2T/CS2_VibeSignatures/issues/946 updated/read back successfully. These are agreed requirements, NOT implemented behavior.
- [decision] Full triggers only for maintained gamesymbol additions/modifications/deletions and effective finder/preprocessor changes, including indirectly referenced helpers. Referenced generator-only changes trigger full even if config/artifact bytes are unchanged. Unreferenced sources run the full test suite only. Consider base and merge consumers for removals/renames.
- [decision] Current-version config comments/whitespace/layout changes with identical parsed configuration run tests only. Shared sig-finder agent definitions are infrastructure and run tests only, as do infrastructure-only changes generally.
- [decision] Remove blanket rejection of changed trust-root paths; preserve immutable base-SHA trusted validator loading and exact prospective-tree/plan binding. Earlier statement that preserving this blanket guard is a non-goal is superseded.
- [decision] Retain existing source parsing errors; normal unreferenced sources must not fall back to broad rebuild. Do not add a general dynamic dependency analysis system or generic unknown-dependency state in this issue.
- [decision] Preserve earlier non-latest config/artifact rejection, existing-version manual binary-identity rejection, new-GAMEVER bootstrap, full test suite, selected execution/inheritance and evidence validation. Full-validation is a route, not synonymous with force-all rebuilding.
- [fact] Current AnalysisSourceIndex uses AST parsing and top-level preprocessor static imports; dynamic/relative imports are not generally covered. This is an implementation limitation, not an existing explicit unknown-dependency error state.

## Implementation: PR #950 (branch `dev-narrow-full-validation`, 2026-09-08)

- [fact] #946 implemented in PR #950. `trusted_artifact_pr.py`: removed the trust-root path guard and the `shared_analysis_changed` force-full; `_is_shared_analysis_runtime_path` survives only for the informational `base/merge_analysis_sources` inventories; the binary-identity force-full became an explicit rejection comparing `binary_lock.download_identity()` (app_id/branch/manifests/os) on both raw download.yaml payloads plus the loaded lock sha256 for gamevers configured on BOTH sides (`gamever in base_versions and gamever in merge_versions` -> "manual binary identity change ... use the download/binary-lock bump flow") — comparing whole download entries instead would wrongly reject non-identity metadata edits such as `major_update: true` (review finding, fixed same PR), so new GAMEVERs keep the bootstrap path and metadata-only edits route `light` with `prior_gamever=None`; new `_reject_non_maintained_version_edits(changes, maintained_versions)` fails closed listing version+paths when any change touches `configs/<ver>.yaml` or `bin_artifacts/<ver>/**` of a non-maintained version (this also fail-closes GAMEVER removal, because the removed version is not in `maintained_versions`, and runs after `_validate_repository_tree_namespaces` so unconfigured-artifact errors keep precedence).
- [fact] `gamesymbol_snapshot_lib/pr_validation.py`: `BROAD_ANALYSIS_FILES` (sig-finder agents) and the contract-version all-node seeding were removed; `_preprocessor_change_nodes` lost its `head_contract` broad-rebuild fallback — an unreferenced source now yields empty nodes plus a `warning: unreferenced analysis source had no producer consumer:` reason, mirroring the orphan-reference warning; Python parse errors still raise `SnapshotConfigError("unable to parse ...")`. Consumers-map behaviors (references, agent skills gated on `by_skill`, base-side deletion consumers) are unchanged.
- [fact] Routing outcomes after #950: infra-only edits (root `*.py`, `gamesymbol_snapshot_lib/**`, workflows, validators, sig-finder agents, contract version) -> `light` (full test suite only); maintained gamesymbol/config/artifact or effective (referenced) finder/preprocessor/helper edits -> `full` with the existing selected execution/inheritance/evidence machinery; non-maintained version edits and manual identity edits -> explicit `TrustedArtifactPrError`; new GAMEVER with missing artifacts -> `bootstrap_required` unchanged. A maintained-config comment-only edit keeps `config_sha256` identical (it is derived from the parsed config) and routes `light`.
- [fact] Tests: `PrValidationRoutingTests` in `tests/test_trusted_artifact_pr.py` is a two-version (1000/2000, +3000 bootstrap) git fixture covering the #946 acceptance matrix; `test_gamesymbol_pr_validation.py` gained the parse-error and deleted-helper base-consumer cases. The old selected-policy "full fallback" test now triggers via an all-artifacts change (`all-artifacts`) because shared-runtime no longer selects all groups.
