interface:
  display_name: "Resolve PR Conflict"
  short_description: "Resolve PR conflicts through a validated base-sync merge"
  default_prompt: "Use $resolve-pr-conflict to resolve an open PR conflict, merge the base into the PR head, run the read-only review audit, and push the PR branch without merging the PR."
