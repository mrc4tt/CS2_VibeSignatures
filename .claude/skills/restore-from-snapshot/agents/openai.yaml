interface:
  display_name: "Restore From Snapshot"
  short_description: "Restore trusted or user-forced symbol YAML snapshots"
  default_prompt: "Use $restore-from-snapshot to restore symbol YAML for one GAMEVER."
policy:
  allow_implicit_invocation: false
