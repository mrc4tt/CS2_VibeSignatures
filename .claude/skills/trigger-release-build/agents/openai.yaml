interface:
  display_name: "Trigger Release Build"
  short_description: "Dispatch a safe release rebuild from origin/main"
  default_prompt: "Use $trigger-release-build to rebuild the requested game version from the current origin/main."
policy:
  allow_implicit_invocation: false
