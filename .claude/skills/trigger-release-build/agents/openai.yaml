interface:
  display_name: "Trigger Release Build"
  short_description: "Safely publish immutable output from origin/main"
  default_prompt: "Use $trigger-release-build to publish the requested game version from the current immutable origin/main source artifacts."
policy:
  allow_implicit_invocation: false
