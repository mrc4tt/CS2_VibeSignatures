interface:
  display_name: "Trigger Release Build"
  short_description: "Safely publish or republish from origin/main"
  default_prompt: "Use $trigger-release-build to publish or republish the requested game version from the current origin/main."
policy:
  allow_implicit_invocation: false
