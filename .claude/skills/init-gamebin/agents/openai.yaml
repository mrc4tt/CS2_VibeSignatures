interface:
  display_name: "Initialize Game Binaries"
  short_description: "Prepare binaries, BinSync recovery, and symbol YAMLs"
  default_prompt: "Use $init-gamebin to initialize local binaries for a selected GAMEVER."
policy:
  allow_implicit_invocation: false
